name = "baradaran-portfolio"
main = "worker/src/index.js"
compatibility_date = "2025-04-01"

# Static Vite build — required for `wrangler versions upload` / Workers Builds
[assets]
directory = "./dist"
binding = "ASSETS"
not_found_handling = "single-page-application"
run_worker_first = ["/api/*"]

[vars]
CONTACT_TO_EMAIL = "baradaran13085@gmail.com"
CONTACT_FROM_EMAIL = "Portfolio <onboarding@resend.dev>"
ALLOWED_ORIGINS = "*"
